<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions untuk sharing dan akses gambar -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <uses-permission android:name="android.permission.WRITE_MEDIA_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- HAPUS: MANAGE_EXTERNAL_STORAGE - Hanay digunakan untuk file manager -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Android 13 version adaptation,细化存储权限 -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- Permission untuk Android 14+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <application
        android:name=".AdminkuApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Adminku"
        tools:targetApi="36">

        <!-- FileProvider untuk sharing files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.bdajaya.adminku.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Adminku.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activities.BrowseCategoryActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan|stateHidden"
            android:theme="@style/Theme.Adminku.NoActionBar" />

        <activity
            android:name=".ui.activities.BrowseUnitActivity"
            android:exported="false"
            android:theme="@style/Theme.Adminku.NoActionBar" />

        <activity
            android:name=".ui.activities.BrowseBrandActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan|stateHidden"
            android:theme="@style/Theme.Adminku.NoActionBar" />

        <activity
            android:name=".ui.activities.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.Adminku.NoActionBar" />

        <activity
            android:name=".ui.activities.ProductManagementActivity"
            android:parentActivityName=".ui.activities.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Adminku.NoActionBar" />

        <activity
            android:name=".ui.activities.AddEditProductActivity"
            android:parentActivityName=".ui.activities.ProductManagementActivity"
            android:windowSoftInputMode="adjustPan|stateHidden"
            android:exported="false"
            android:theme="@style/Theme.Adminku.NoActionBar" />
    </application>

</manifest>